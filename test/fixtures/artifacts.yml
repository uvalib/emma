# test/fixtures/artifacts.yml
#
# @see http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# =============================================================================
# One artifact for each unique format type.
# =============================================================================

daisy:
  format: DAISY

daisy_audio:
  format: DAISY_AUDIO

daisy_segmented:
  format: DAISY_SEGMENTED

brf:
  format: BRF

epub:
  format: EPUB3

pdf:
  format: PDF

docx:
  format: DOCX

html:
  format: HTML

text:
  format: TEXT

# =============================================================================
# Examples
# =============================================================================

example:
  format: BRF

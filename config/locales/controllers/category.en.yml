# config/locales/controllers/category.en.yml
#
# CategoryController pages

en:

  emma:

    category:

      unit:
        brief:          'category'
        long:           'catalog category'
      label:            'Categories'
      title:            'Categories'
      tooltip:          'Search for Bookshare catalog items by topic.'

      # =======================================================================
      # Layout
      # =======================================================================

      search_bar:
        enabled:        false

      search_controls:

        size:
          tooltip:      'Specify the number of categories per page'
          values:       [10, 25, 50, 100]

        layout:
          - [size, ~, ~, ~]

      # =======================================================================
      # Controls
      # =======================================================================

      pagination:
        page_size:      25

      # =======================================================================
      # Fields
      # =======================================================================

      fields:           &CATEGORY_FIELDS {}

      # =======================================================================
      # Pages
      # =======================================================================

      index:
        label:          'Categories'
        title:          'Bookshare Category Index'
        tooltip:        'Get a Bookshare category listing.'
        fields:
          <<:           *CATEGORY_FIELDS

      show:
        label:          'Category'
        title:          'Bookshare Category'
        tooltip:        'See Bookshare items in this category.'
        fields:
          <<:           *CATEGORY_FIELDS

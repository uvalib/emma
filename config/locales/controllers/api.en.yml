# config/locales/controllers/api.en.yml

en:

  emma:

    # =========================================================================
    # API page
    # =========================================================================

    api:

      label:            'API Methods'
      title:            'Bookshare API Methods'
      tooltip:          'Bookshare API methods accessed as the current user.'

      # =======================================================================
      # Pages
      # =======================================================================

      index:
        label:          'API Methods'
        title:          'Bookshare API Methods'
        tooltip:        'Bookshare API methods accessed as the current user.'

      v2:
        label:          'API Method'
        title:          'Bookshare API Method Output'
        tooltip:        'The response from a Bookshare API method accessed as the current user.'

# config/locales/controllers/search_call.en.yml
#
# SearchCallsController pages

en:

  emma:

    search_call:

#     # NOTE: Some of these definitions require parameters to be supplied:
#     #
#     # %{terms}        Current list of search terms.

#     unit: # NOTE: to "en.emma.page.search_call"
#       brief:          '/x/ call'
#       long:           '/x/ search call record'
#     label:            '/x/ Searches' # NOTE: to "en.emma.page.search_call"
#     title:            '/x/ Search Calls' # NOTE: to "en.emma.page.search_call"
#     tooltip:          '/x/ List search call records' # NOTE: to "en.emma.page.search_call"

      # =======================================================================
      # Subsystems
      # =======================================================================

#     subsystem:
#       database:       true
#       search:         true
#       redis:          false # NOTE: added to facilitate comparison
#       storage:        false # NOTE: added to facilitate comparison
#       ingest:         false # NOTE: added to facilitate comparison

      # =======================================================================
      # Search endpoint
      # =======================================================================

#     search: # NOTE: to "en.emma.page._generic.search"
#       action:         index

      # =======================================================================
      # Layout
      # =======================================================================

#     search_type: # NOTE: to "en.emma.page.search_call.search_type"

#       like:
#         name:         '/x/ Filter'
#         label:        '/x/ Filter by query or filter:'
#         placeholder:  '/x/ Enter query or filter...'

#     search_bar: # NOTE: to "en.emma.page.search_call.search_bar"
#       enabled:        [index]
#       enabled:        false
#       label:          '/x/ Search Call Filter'

#       min_rows:         1 # NOTE: added to facilitate comparison
#       max_rows:         1 # NOTE: added to facilitate comparison

#       button: # NOTE: added to facilitate comparison
#         label:          '/x/ Search'

#       advanced: # NOTE: added to facilitate comparison
#         label:          '/x/ More ▼'
#         tooltip:        '/x/ Show additional search filters'
#         open:
#           label:        '/x/ Less ▲'
#           tooltip:      '/x/ Hide additional search filters'

#     search_filters: # NOTE: to "en.emma.page.search_call.search_filters"
#       enabled:        [index]
#       enabled:        false

#       layout:
#         - [sort, size, ~, ~]

#     page_controls: # NOTE: to "en.emma.page.search_call.page_controls"
#       label:
#         one:          '/x/ Manage this record:'
#         many:         '/x/ Record Actions:'

      # =======================================================================
      # Pages
      # =======================================================================

#     index: # NOTE: to "en.emma.page.search_call.action.index"
#       label:          '/x/ Search Calls'
#       title:          '/x/ Search Call Records'
#       tooltip:        '/x/ List search call records'

#       display_fields: # @see "en.emma.record.search_call" # NOTE: to "en.emma.page.search_call.action.index.display_fields"
#         - !ruby/symbol all

#       _endpoint:      true

      # =======================================================================

#     show: # NOTE: to "en.emma.page.search_call.action.show"
#       label:          '/x/ Search Call'
#       title:          '/x/ Search Call Record'
#       tooltip:        '/x/ See details about this search call record'

#       display_fields: # @see "en.emma.record.search_call" # NOTE: to "en.emma.page.search_call.action.show.display_fields"
#         - !ruby/symbol all

#       _endpoint:      true

      # =======================================================================

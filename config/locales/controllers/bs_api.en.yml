# config/locales/controllers/bs_api.en.yml
#
# BsApiController pages

en:

  emma:

    bs_api:

      label:            'Bookshare API'
      title:            'Bookshare API Methods'
      tooltip:          'Bookshare API methods accessed as the current user.'

      # =======================================================================
      # Layout
      # =======================================================================

      search_bar:
        enabled:        false

      search_filters:
        enabled:        false

      # =======================================================================
      # Pages
      # =======================================================================

      index:
        label:          'Bookshare API'
        title:          'Bookshare API Methods'
        tooltip:        'Bookshare API methods accessed as the current user.'
        _endpoint:      true

      # =======================================================================

      v2:
        label:          'BS API Method'
        title:          'Bookshare API Method Output'
        tooltip:        'The response from a Bookshare API method accessed as the current user.'
        _endpoint:      true

      # =======================================================================

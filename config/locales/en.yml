# config/locales/en.yml

en:

  emma:

    application:
      name:             'EMMA'

    user:

      sessions:
        new:
          action:       'Sign in'
          title:        'Sign in with your Bookshare account'
          submit:       'Sign in'

      passwords:
        new:
          action:       'Forgot your password?'

      registrations:
        new:
          action:       'Sign up'

      confirmations:
        new:
          action:       "Didn't receive confirmation instructions?"

      unlocks:
        new:
          action:       "Didn't receive unlock instructions?"

      omniauth_callbacks:
        new:
          action:       'Sign in with %{provider}'

    home:
      title:            'EMMA Test Application'

      welcome:
        title:          'Welcome to EMMA'

      dashboard:
        title:          'EMMA Dashboard'

    api:
      title:            'Bookshare API Methods'

    health:

      mysql:
        restart:        true
        failed:         'Database connection error'

      redis:
        restart:        true
        healthy:        'NOT IMPLEMENTED' # TODO: future
        failed:         'Redis unresponsive'

      bookshare:
        restart:        false
        degraded:       'Bookshare API degraded'
        failed:         'Bookshare API unresponsive'

      ingest:
        restart:        false
        healthy:        'NOT IMPLEMENTED' # TODO: future
        degraded:       'Content ingest degraded'
        failed:         'Content ingest failing'

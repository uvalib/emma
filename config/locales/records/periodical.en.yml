# config/locales/records/periodical.en.yml
#
# Periodical model fields.
#
# NOTE: The order here defines the order for "display_fields: !ruby/symbol all"
# in config/locales/controllers/periodical.en.yml

en:

  emma:

    periodical:

      record:

        # =====================================================================
        # Description
        # =====================================================================

        title:
          label:              'Title'

        year:
          label:              'Year'

        languages:
          label:              'Languages'

        description:
          label:              'Description'

        categories:
          label:              'Categories'

        countries:
          label:              'Countries'

        # =====================================================================
        # Identifiers
        # =====================================================================

        issn:
          label:              'ISSN'

        externalCategoryCode:
          label:              'External Category Code'

        # =====================================================================
        # Publisher/provider information
        # =====================================================================

        publisher:
          label:              'Publisher'

        # =====================================================================
        # Item instances
        # =====================================================================

        latestEdition:
          label:              'Latest Edition'

        editionCount:
          label:              'Editions'

        # =====================================================================
        # Bookshare information
        # =====================================================================

        seriesId:
          label:              'Series ID'

        links:
          label:              'Links'

# config/locales/formats/epub.en.yml
#
# EPUB format

en:

  emma:

    epub:

      type: 'epub'

      mimes:
        - 'application/epub+zip'
        - 'application/epub-file'

      exts:
        - 'epub'
        - 'epub3'

      # =======================================================================
      # FORMAT_FIELDS
      # =======================================================================

      # NOTE: @see ocf.en.yml

      # =======================================================================
      # A mapping of format field to the equivalent
      # Search::Record::MetadataRecord field.
      # =======================================================================

      # NOTE: @see ocf.en.yml

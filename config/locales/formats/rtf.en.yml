# config/locales/formats/rtf.en.yml
#
# RTF (Rich Text Format)

en:

  emma:

    rtf:

      type: 'rtf'

      mimes:
        - 'application/rtf'

      exts:
        - 'rtf'

      # =======================================================================
      # FORMAT_FIELDS
      # =======================================================================

      fields:

        # NONE SO FAR

      # =======================================================================
      # A mapping of format field to the equivalent
      # Search::Record::MetadataRecord field.
      # =======================================================================

      map:

        # NONE SO FAR

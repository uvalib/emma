# config/locales/formats/kurzweil.en.yml
#
# Kurzweil 3000 Document format - TODO: ?

en:

  emma:

    kurzweil:

      type: 'kurzweil'

      mimes:
        - 'application/x-kurzweil' # NOTE: fake MIME type

      exts:
        - 'kes'
        - 'kurzweil'

      # =======================================================================
      # FORMAT_FIELDS
      # =======================================================================

      fields:

        # NONE SO FAR

      # =======================================================================
      # A mapping of format field to the equivalent
      # Search::Record::MetadataRecord field.
      # =======================================================================

      map:

        # NONE SO FAR

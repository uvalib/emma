# config/locales/formats/grayscale_pdf.en.yml
#
# Grayscale PDF - TODO: Should this really be a distinct "format"?

en:

  emma:

    grayscalePdf:

      type: 'grayscalePdf'

      mimes:
        - 'application/x-grayscale-pdf+zip'   # NOTE: fake MIME type
        - 'application/x-grayscale-pdf-file'  # NOTE: fake MIME type

      exts:
        - 'grayscalePdf'                      # TODO: ???
        - 'pdf'

      # =======================================================================
      # FORMAT_FIELDS
      # =======================================================================

      # NOTE: @see pdf.en.yml

      # =======================================================================
      # A mapping of format field to the equivalent
      # Search::Record::MetadataRecord field.
      # =======================================================================

      # NOTE: @see pdf.en.yml

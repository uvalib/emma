# config/locales/formats/braille.en.yml
#
# Braille format - TODO: Is this actually different than BRF?
#
# Braille is basically a type of text document; any bibliographic metadata such
# document might contain would be highly dependent on arbitrary conventions.

en:

  emma:

    braille:

      type: 'braille'

      mimes:
        - 'application/x-braille' # NOTE: fake MIME type

      exts:
        - 'braille'

      upload:
        parse:          false

      # =======================================================================
      # FORMAT_FIELDS
      # =======================================================================

      fields:

        # NONE SO FAR

      # =======================================================================
      # A mapping of format field to the equivalent
      # Search::Record::MetadataRecord field.
      # =======================================================================

      map:

        # NONE SO FAR

<%# app/views/user/registrations/edit_select.html.erb -%>
<%-
  # Select a user to edit.

  view_debug(__FILE__)

  dec     = UsersDecorator.new(**context(:edit))

  help  ||= nil # %i[account edit_select]
  title ||= 'Select an EMMA user account' # TODO: I18n

  unless modal?
    set_page_meta_title(title)
    set_page_description(title)
  end

-%>

<%= page_heading(title, help: help) %>
<%= page_description_section %>

<%= page_directions_section %>
<%= dec.items_menu %>

<%= page_notes_section %>
<%= render 'devise/shared/links' unless modal? %>

<%# app/views/user/sessions/sign_in_local.html.erb -%>
<%-
  # Sign in with an EMMA user account encrypted password.

  view_debug(__FILE__)

  cfg   ||= config_page_section(:user_sessions, :sign_in)
  help  ||= nil
  title ||= cfg[:title] || cfg[:label] || 'Sign in'

  unless modal?
    set_page_meta_title(title)
    set_page_description(title)
  end

-%>

<%= page_heading(title, help: help) %>
<%= page_description_section %>

<%= page_directions_section %>
<%= render 'user/sessions/local', page: true %>

<%= page_notes_section %>

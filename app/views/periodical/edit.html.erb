<%# app/views/periodical/edit.html.erb -%>
<%-
  # Periodical edit form.
  title ||= t('emma.periodical.edit.title')

  set_page_title(title)
  set_page_description(title)
  set_page_robots(:noindex)

  name = current_user.to_s.inspect
  can  = can?(:edit, Periodical) ? 'can' : 'can NOT'
-%>

<h1 class="heading"><%= title -%></h1>

<p>(User <%= name -%> <%= can -%> modify periodical metadata.)</p>

<p>TODO</p>

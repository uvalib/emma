<%# app/views/periodical/_list.html.erb -%>
<%-
  # Periodical list.
  list ||= []
  skip ||= nil

  skip = Array.wrap(skip)
  heading =
    unless skip.include?(:title)
      title ||= t('emma.periodical.index.title')
      unless skip.include?(:count)
        count ||= list.size
        title += " (#{count})"
      end
      content_tag(:h2, h(title), class: 'list-heading')
    end
-%>

<%= heading %>

<%- if list.present? -%>
  <%- list.each_with_index do |item, idx| -%>
    <%= render 'periodical/entry', index: idx, item: item %>
  <%- end -%>
<%- else -%>
  <%= render 'periodical/entry', index: -1 %>
<%- end -%>

<%# app/views/account_mailer/new_org_email.text.erb -%>
<%-
  # Create a welcome message to the manager of a new organization.
  elem ||= @elem
  body ||= elem[:body]
-%>
<%= as_text(body) || render_as_text('new_org') %>

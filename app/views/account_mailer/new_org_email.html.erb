<%# app/views/account_mailer/new_org_email.html.erb -%>
<%-
  # Create a welcome message to the manager of a new organization.
  elem ||= @elem
  heading, body = elem.values_at(:heading, :body)
  body &&= html_h1(heading) << body if heading.present?
-%>
<%= body || render('new_org') %>

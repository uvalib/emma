<%# app/views/account_mailer/welcome_email.text.erb -%>
<%-
  # Create a new user welcome message.
  elem ||= @elem
  head, body = elem.values_at(:heading, :body)
-%>

<%= head %>
<%= '=' * head.size %>

<%= body.join("\n\n") %>

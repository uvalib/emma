<%# app/views/entry/bulk_reindex.html.erb -%>
<%-
  # Results of re-indexing.
  title ||= t('emma.entry.bulk_reindex.title')
  list  ||= @list || []
-%>

<%= render 'fallback', title: title, error: 'Items:' %>

<ul>
  <%- list.each do |item| -%>
    <li><%= Entry.sid_value(item) -%></li>
  <%- end -%>
</ul>

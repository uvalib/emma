<%# app/views/manifest/_details.html.erb -%>
<%-
  # A listing of a Manifest record details.

  view_debug(__FILE__)

  item  ||= nil
  dec   ||= item && ManifestDecorator.new(item, **context) or return

  level ||= 1
  skip  ||= nil

-%>

<%= dec.details_container(level: level, skip: skip) %>

<%# app/views/manifest/_menu.html.erb -%>
<%-
  # Show a menu of manifests to operate on driven by params[:action].

  view_debug(__FILE__)

  dec ||= ManifestDecorator.new(**context)
  opt   = { dec: dec }
-%>

<%= render 'layouts/menu_page', **opt %>

<%# app/views/tool/index.html.erb -%>
<%-
  # Trial pages

  view_debug(__FILE__)

  title ||= t('emma.tool.index.title')
  help  ||= nil # :tool

  unless modal?
    set_page_meta_title(t('emma.tool.index.label', default: title))
    set_page_description(title)
  end

-%>

<%= page_heading(title, help: help) %>
<%= page_description_section %>

<%= page_directions_section %>
<ul class="tool-list">
  <li class="tool-item"><%= link_to('Math Detective', md_trial_path) -%></li>
</ul>

<%= page_notes_section %>

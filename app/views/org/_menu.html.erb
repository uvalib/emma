<%# app/views/org/_menu.html.erb -%>
<%-
  # Show a menu of EMMA organizations to operate on driven by params[:action].

  view_debug(__FILE__)

  dec ||= OrgDecorator.new(**context)
  opt   = { dec: dec }
-%>

<%= render 'layouts/menu_page', **opt %>

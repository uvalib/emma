<%# app/views/artifact/new.html.erb -%>
<%-
  # New artifact form.
  title ||= t('emma.artifact.new.title')
  @page_title = title
  can = current_user.is_artifact_submitter? ? 'can' : 'can NOT'
-%>

<h1 class="heading"><%= title -%></h1>

<p>(User <%= current_user.to_s.inspect -%> <%= can -%> create.)</p>

<p>TODO</p>

<%# app/views/artifact/_details.html.erb -%>
<%-
  # An Artifact metadata listing.

  view_debug(__FILE__)

  item  ||= nil
  dec   ||= item && ArtifactDecorator.new(item, **context) or return

  level ||= 1
  extra ||= nil
  skip  ||= nil

-%>

<%= dec.details_container(level: level, pairs: extra, skip: skip) %>

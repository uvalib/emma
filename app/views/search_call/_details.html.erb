<%# app/views/search_call/_details.html.erb -%>
<%-
  # A single search call record.

  view_debug(__FILE__)

  item    ||= nil
  dec     ||= item && SearchCallDecorator.new(item, **context) or return

  add     ||= nil
  columns ||= nil
  filter  ||= nil

-%>

<%= dec.details(pairs: add, columns: columns, filter: filter) %>

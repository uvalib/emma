<%# app/views/edition/_list.html.erb -%>
<%-
  # A periodical Edition listing.
  list   ||= []
  count  ||= list.size
  offset ||= page_offset
  skip   ||= nil

  skip = Array.wrap(skip)
  heading =
    unless skip.include?(:title)
      title ||= t('emma.edition.index.title')
      title  += " (#{count} total)" unless skip.include?(:count)
      classes = 'list-heading'
      classes = css_classes(classes, 'empty') if list.blank?
      content_tag(:h2, h(title), class: classes)
    end
-%>

<%= heading %>

<%- if list.present? -%>
  <%- list.each_with_index do |item, idx| -%>
    <%= render 'edition/entry', index: idx, item: item, offset: offset %>
  <%- end -%>
<%- else -%>
  <%= render 'edition/entry', index: -1 %>
<%- end -%>

<%# app/views/edition/new.html.erb -%>
<%-
  # New edition form.
  title ||= t('emma.edition.new.title')

  set_page_title(title)
  set_page_description(title)
  set_page_robots(:noindex, :nofollow)

  name = current_user.to_s.inspect
  can  = can?(:new, Edition) ? 'can' : 'can NOT'
-%>

<h1 class="heading"><%= title -%></h1>

<p>(User <%= name -%> <%= can -%> add an edition.)</p>

<p>TODO</p>

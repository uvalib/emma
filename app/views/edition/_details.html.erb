<%# app/views/edition/_details.html.erb -%>
<%-
  # A periodical Edition metadata listing.

  view_debug(__FILE__)

  item  ||= nil
  dec   ||= item && EditionDecorator.new(item, **context) or return

  add   ||= nil
  level ||= 1

-%>

<%= dec.details_element(pairs: add, level: level) %>

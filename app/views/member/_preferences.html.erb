<%# app/views/member/_preferences.html.erb -%>
<%-
  # Member preferences listing.
  item ||= @pref
  return if item.blank?
-%>

<div class="member-preferences">
  <%= member_preference_values(item) %>
</div>

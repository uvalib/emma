<%# app/views/member/_list.html.erb -%>
<%-
  # List list.
  list ||= []
  skip ||= nil

  skip = Array.wrap(skip)
  heading =
    unless skip.include?(:title)
      title ||= t('emma.member.index.title')
      unless skip.include?(:count)
        count ||= list.size
        title += " (#{count})"
      end
      content_tag(:h2, h(title), class: 'list-heading')
    end
-%>

<%= heading %>

<%- if list.present? -%>
  <%- list.each_with_index do |item, idx| -%>
    <%= render 'member/entry', index: idx, item: item %>
  <%- end -%>
<%- else -%>
  <%= render 'member/entry', index: -1 %>
<%- end -%>

<%# app/views/member/new.html.erb -%>
<%-
  # New Member form.
  title ||= t('emma.member.new.title')
  @page_title = title
  can = current_user.is_membership_manager? ? 'can' : 'can NOT'
-%>

<h1 class="heading"><%= title -%></h1>

<p>(User <%= current_user.to_s.inspect -%> <%= can -%> create.)</p>

<p>TODO</p>

<%# app/views/reading_list/edit.html.erb -%>
<%-
  # Reading List edit form.
  title ||= t('emma.reading_list.edit.title')

  set_page_title(title)
  set_page_description(title)
  set_page_robots(:noindex)

  name = current_user.to_s.inspect
  can  = can?(:edit, ReadingList) ? 'can' : 'can NOT'
-%>

<h1 class="heading"><%= title -%></h1>

<p>(User <%= name -%> <%= can -%> modify reading list.)</p>

<p>TODO</p>

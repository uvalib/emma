<%# app/views/reading_list/page_controls/_show.html.erb -%>
<%-
  # Page controls on a Reading List show page for an authorized user.
  actions = page_control_actions(ReadingList, :edit, :delete)
  # TODO: add title / remove title -- or should this only be on item page?
  return if actions.blank?

  anchor_id = 'show-page-controls'
  skip_nav_prepend(reading_list: anchor_id)

  id ||= params[:id]

  path_opt = { id: id, ids: [id] }
-%>

<div class="label" id="<%= anchor_id -%>"><%= page_controls_label -%>:</div>
<div class="controls"><%= page_controls(*actions, **path_opt) %></div>

<%# app/views/reading_list/_details.html.erb -%>
<%-
  # A Reading List metadata listing with associated titles.

  view_debug(__FILE__)

  item  ||= nil
  list  ||= nil
  dec   ||= item && ReadingListDecorator.new(item, titles: list, **context) or return

  add   ||= nil
  level ||= 1
  skip  ||= nil

-%>

<%= dec.details_element(pairs: add, level: level, skip: skip) %>

<%# app/views/title/_list.html.erb -%>
<%-
  # A list of catalog Titles.
  list   ||= []
  count  ||= list.size
  offset ||= page_offset
  skip   ||= nil

  skip = Array.wrap(skip)
  heading =
    unless skip.include?(:title)
      title ||= t('emma.title.index.title')
      title  += " (#{count})" unless skip.include?(:count)
      classes = 'list-heading'
      classes = css_classes(classes, 'empty') if list.blank?
      content_tag(:h2, h(title), class: classes)
    end
-%>

<%= heading %>

<%- if list.present? -%>
  <%- list.each_with_index do |item, idx| -%>
    <%= render 'title/entry', index: idx, item: item, offset: offset %>
  <%- end -%>
<%- else -%>
  <%= render 'title/entry', index: -1 %>
<%- end -%>

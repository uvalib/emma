<%# app/views/title/new.html.erb -%>
<%-
  # New Catalog Title form.
  title ||= t('emma.title.new.title')

  set_page_title(title)
  set_page_description(title)
  set_page_robots(:noindex)

  name = current_user.to_s.inspect
  can  = can?(:new, Title) ? 'can' : 'can NOT'
-%>

<h1 class="heading"><%= title -%></h1>

<p>(User <%= name -%> <%= can -%> add a catalog title.)</p>

<p>TODO</p>

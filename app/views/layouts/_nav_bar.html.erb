<%# app/views/layouts/_nav_bar.html.erb -%>
<%-
  # Navigation bar.
  skip ||= !show_nav_bar?
  return if skip || (controls = nav_bar_links).blank?
-%>

<nav class="layout-navbar">
  <%= controls %>
</nav>

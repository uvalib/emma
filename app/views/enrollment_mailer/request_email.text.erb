<%# app/views/enrollment_mailer/request_email.text.erb -%>
<%-
  # Create a new EMMA enrollment request ticket.
  elem ||= @elem
  head, body = elem.values_at(:heading, :body)
-%>

<%= head %>
<%= '=' * head.size %>

<%= body.join("\n\n") %>

<%# app/views/account/_menu.html.erb -%>
<%-
  # Show a menu of EMMA user accounts to operate on driven by params[:action].

  view_debug(__FILE__)

  dec ||= AccountDecorator.new(**context)
  opt   = { dec: dec }
-%>

<%= render 'layouts/menu_page', **opt %>

<%# app/views/account/_details.html.erb -%>
<%-
  # Local EMMA user account details.

  view_debug(__FILE__)

  user    ||= nil
  dec     ||= user && AccountDecorator.new(user, **context) or return

  add     ||= nil
  columns ||= nil
  filter  ||= nil

-%>

<%= dec.details(pairs: add, columns: columns, filter: filter) %>

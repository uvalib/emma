<%# app/views/account/edit.html.erb -%>
<%-
  # Modify an existing local EMMA user account.

  view_debug(__FILE__)

  item  ||= @item
  dec     = AccountDecorator.new(item, **context)

  title ||= dec.page_heading
  help  ||= nil # dec.help_topic

  unless modal?
    label = dec.page_meta_title(default: title)
    set_page_meta_title(label)
    set_page_description(title)
    set_page_robots(:noindex, :nofollow)
  end

-%>

<%= page_heading(title, help: help) %>
<%= page_description_section %>

<%- if dec.blank? -%>

  <%= dec.items_menu %>

<%- else -%>

  <%= page_directions_section %>
  <%= dec.model_form %>

<%- end -%>

<%= page_notes_section %>
<%= dec.action_list unless modal? %>

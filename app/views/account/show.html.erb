<%# app/views/account/show.html.erb -%>
<%-
  # Show details of a local EMMA user account.

  view_debug(__FILE__)

  item  ||= @item
  dec     = AccountDecorator.new(item, **context)
  item  ||= dec.object

  help  ||= nil # dec.help_topic
  title ||= nil
  title   = dec.page_heading(title, name: item.uid)

  unless modal?
    label = "#{dec.page_meta_title} - #{title}"
    set_page_meta_title(label)
    set_page_description(title)
  end

  opt = { dec: dec }

-%>

<%= page_heading(title, help: help) %>
<%= page_description_section %>

<%= page_directions_section %>
<%= render 'account/details', opt %>

<%= page_notes_section %>
<%= dec.action_list unless modal? %>

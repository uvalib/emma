<%# app/views/data/_details.html.erb -%>
<%-
  # The contents of a database table.

  view_debug(__FILE__)

  item ||= []
  name ||= nil or view_abort(__FILE__, ':name not passed via "locals"')
  cols ||= nil

-%>

<%= html_db_table(item, name: name, cols: cols) %>
